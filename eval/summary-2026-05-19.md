# Evaluation Summary — 2026-05-19

## Setup

| Parameter     | Value |
|---------------|-------|
| Date          | 2026-05-19 |
| Repo SHA      | `36aa43d9f8ab263dbcd06e251fe1f3bec4fb96c7` |
| Kernel        | `6.12.62+rpt-rpi-v8` |
| Architecture  | aarch64 (Raspberry Pi 4, Cortex-A72) |
| CPU governor  | `ondemand` |
| N per case    | 50 |
| Timing method | Python `time.monotonic()` (wall clock) around `subprocess.run()` |
| Attack payload| Pre-generated once, reused across N runs (deterministic exploits, no PIE) |

**pwntools startup** (python3 -c 'import pwn', N=10): 944 ± 67.4 ms. This is NOT included in overhead ratio or time-to-detection figures (payload is pre-generated; only tracer+victim time is counted).

## Detection Rates

| Attack | Type | Expected | Detection Rate | Notes |
|--------|------|----------|----------------|-------|
| `01-stack-bof` | Detected | Detect (exit 2) | 100.0% (50/50) |  |
| `02-rop` | Detected | Detect (exit 2) | 100.0% (50/50) |  |
| `03-jop` | Detected | Detect (exit 2) | 100.0% (50/50) |  |
| `04-data-only` | Gap demo | Miss (gap) | 0.0% (0/50) — expected 0% | L1 (control-flow) detector cannot observe data-only attacks:... |

## False Positive Rates (benign runs, tracer active)

| Attack | FPR | False Alerts / N |
|--------|-----|-----------------|
| `01-stack-bof` | 0.00% | 0/50 |
| `02-rop` | 0.00% | 0/50 |
| `03-jop` | 0.00% | 0/50 |
| `04-data-only` | 0.00% | 0/50 |

## Wall-Clock Overhead (benign payload, tracer vs. victim-alone)

| Attack | Victim alone | Tracer+Victim | Overhead ratio (mean) |
|--------|-------------|---------------|----------------------|
| `01-stack-bof` | 4.0 ± 1.0 ms (p50=3.7, p95=5.9) | 9.7 ± 3.4 ms (p50=8.8, p95=16.2) | 2.41x |
| `02-rop` | 3.5 ± 0.7 ms (p50=3.4, p95=4.2) | 9.4 ± 3.7 ms (p50=7.3, p95=16.0) | 2.65x |
| `03-jop` | 4.0 ± 1.3 ms (p50=3.5, p95=6.4) | 9.0 ± 3.4 ms (p50=7.4, p95=14.8) | 2.23x |
| `04-data-only` | 4.0 ± 1.5 ms (p50=3.3, p95=6.9) | 12.6 ± 4.2 ms (p50=10.7, p95=21.4) | 3.13x |

## Time-to-Detection (tracer+victim, attack payload)

| Attack | Tracer+Victim (attack) | Detected | Note |
|--------|----------------------|----------|------|
| `01-stack-bof` | 7.6 ± 2.0 ms (p50=7.1, p95=12.2) | Yes | Tracer wall only (payload pre-generated; excludes Python startup) |
| `02-rop` | 7.8 ± 2.2 ms (p50=7.1, p95=11.7) | Yes | Tracer wall only (payload pre-generated; excludes Python startup) |
| `03-jop` | 9.8 ± 3.3 ms (p50=9.3, p95=14.8) | Yes | Tracer wall only (payload pre-generated; excludes Python startup) |
| `04-data-only` | 29.8 ± 8.8 ms (p50=25.6, p95=49.2) | No (expected) | L1 (control-flow) detector cannot observe data-only attacks: the exploit overwri... |

## Per-Attack Explanations

### 01-stack-bof

Stack buffer overflow overwrites the saved return address in main's stack frame. The shadow call stack records the legitimate return address when `bl vuln` executes; on `ret` from vuln back to main, main's epilogue `ret` is checked and the forged value is caught immediately (at depth 0, before `win()` can execute). Answers RQ-1: detection rate 100%; RQ-2: FPR 0%; RQ-3: overhead documented above.

### 02-rop

Three-gadget ROP chain exploits the same overflow primitive. The very first `ret` in the chain returns to a gadget address rather than the legitimate call site, which the shadow call stack rejects. This confirms that the shadow call stack is structurally sufficient against any ret-based hijack regardless of chain length. Answers RQ-1: detection rate 100%.

### 03-jop

Jump-oriented programming (JOP) attack uses `blr x0` to redirect to a gadget function, then `br x16` onward to win(). No `ret` instruction is forged, so a shadow-stack-only detector cannot catch it. The iter-3b CFG-edge validator (per-call-site indirect-call target set) detects the pivot because jop_gadget is never a materialised call target at the blr site. Answers RQ-1: detection rate 100% (with CFG-edge check).

### 04-data-only

Non-control-data attack. The exploit writes exactly 36 bytes: 32 for u.name and 4 to set u.is_admin = 1. Saved x29/x30 are never touched. The `bl admin_panel` branch is a valid CFG edge (benign code could legitimately reach it if is_admin were truly set), so neither the shadow call stack nor the CFG-edge validator fires. Detection requires L2 data-provenance tracking or L3 spatial memory safety, both outside the scope of the L1 control-flow detector. This is the canonical non-control-data attack (Chen, Xu, Sezer, Gauriar, Iyer, USENIX Security 2005). Detection rate = 0% as expected. Answers RQ-1 by documenting the gap, not a failure.

## Research Question Mapping

| RQ | Metric | Value |
|----|--------|-------|
| RQ-1 (detection) | `01-stack-bof` detection rate | 100.0% (N=50) |
| RQ-2 (FPR)       | `01-stack-bof` benign FPR     | 0.00% (N=50) |
| RQ-3 (overhead)  | `01-stack-bof` overhead ratio  | 2.41x |
| RQ-1 (detection) | `02-rop` detection rate | 100.0% (N=50) |
| RQ-2 (FPR)       | `02-rop` benign FPR     | 0.00% (N=50) |
| RQ-3 (overhead)  | `02-rop` overhead ratio  | 2.65x |
| RQ-1 (detection) | `03-jop` detection rate | 100.0% (N=50) |
| RQ-2 (FPR)       | `03-jop` benign FPR     | 0.00% (N=50) |
| RQ-3 (overhead)  | `03-jop` overhead ratio  | 2.23x |
| RQ-1 (detection) | `04-data-only` detection rate | 0.0% (N=50) |
| RQ-2 (FPR)       | `04-data-only` benign FPR     | 0.00% (N=50) |
| RQ-3 (overhead)  | `04-data-only` overhead ratio  | 3.13x |

